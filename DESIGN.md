# Joy Portfolio Design

## 장면과 방향
채용 담당자가 밝은 사무실의 데스크톱이나 이동 중인 휴대폰으로 프로젝트를 살펴본다. 기존 검정·흰색·레드 포트폴리오를 유지하되 읽는 영역에는 충분한 대비와 여백을 둔다. 목소리는 단단함, 직접성, 영화 포스터의 스케일이다.

## 색
검정 잉크 `oklch(0.14 0.005 25)`, 흰 종이 `oklch(0.96 0.005 65)`, 깊은 레드 `oklch(0.38 0.16 27)`, 밝은 레드 `oklch(0.69 0.19 27)`. 새 본문 영역에만 위 색을 적용한다. 히어로는 원본 `--ink: #050505`, `--paper: #f2f2f2`, `--red: #7a0c07`을 유지한다.

## 활자와 구성
기존 Arial/Helvetica와 Georgia 디스플레이 대비를 보존한다. 한국어는 Apple SD Gothic Neo/Malgun Gothic 계열 시스템 폰트를 사용한다. 외부 폰트 요청은 없다. 큰 영문 제목은 조밀하게, 한국어 본문은 1.7 행간으로 구성한다.
풀스크린 히어로 → 작업 인덱스 → 세 개의 큰 프로젝트 장면 → 경력 타임라인 → 이전 작업 기록 → 학력 원본 링크 → 연락. 상세 기여는 목록과 선으로 구분하고 모달 없이 본문에서 읽는다.

## 모션과 접근성
히어로는 `9a4ec88`의 820dvh 무대, 7개 카드 확장, CSS1 문 열림·텍스트 양쪽 분할, 기존 스택 퇴장과 텍스트 도킹, 붉은 커튼 분할, easing, 상시 requestAnimationFrame 및 0.12 보간을 그대로 사용한다. 780px·1023px 원본 반응형 규칙과 `?p=` 디버그 파라미터를 보존하며, 화면 크기로 모션을 끄지 않는다. 히어로는 모션 감소 설정에서도 최신 prod 동작을 보존한다. 추가 본문에만 prefers-reduced-motion 규칙을 적용한다. 새 내비게이션과 본문은 히어로 아래 `.portfolio-content`에 한정한다. skip link, 명확한 focus-visible, 앵커 여백, 새 창 링크 안내를 제공한다.
